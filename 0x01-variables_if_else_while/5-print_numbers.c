#include <stdio.h>

/**
 * main - a program that print the numbers of base 10 starting from 0
 * Return: 0 (success)
 */

int main(void)
{
	int nbr;

	while (n < 10)
	{
		printf("%d", nbr);
		nbr++;
	}
	printf("\n");
	return (0);
}
